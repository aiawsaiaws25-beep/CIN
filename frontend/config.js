// Points the frontend at a backend API. Leave as '' when the frontend and
// backend are served from the same origin (e.g. `node backend/server.js`).
// When hosting the frontend statically (e.g. GitHub Pages), deploy the
// backend/ folder to a Node host (Render, Railway, Fly.io, etc.) and set
// this to that server's URL, e.g. 'https://cinebook-api.onrender.com'.
window.CINEBOOK_API_BASE = '';
